{
  "name": "@copium/settlement-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --import tsx src/worker.ts",
    "settle-once": "node --import tsx src/worker.ts --once"
  },
  "dependencies": {
    "@copium/db": "workspace:*",
    "@copium/settlement": "workspace:*",
    "@copium/txline": "workspace:*",
    "dotenv": "^16.5.0",
    "ioredis": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "tsx": "^4.20.3",
    "typescript": "^5.9.3"
  }
}
